import {Component, OnInit} from '@angular/core';import {Wakanda} from '../wakanda.service';@Component({  providers: [Wakanda],  selector: 'personal-tab',  templateUrl: 'PersonalTab.html',})export class PersonalTab implements OnInit{		travelers =[];		firstname: string;	surname: string; 	mobile: string; 	homePhone: string;	passportNumber: string; 	passportCountry: string;	birthCountry: string;	dateOfBirth: Date;	emailAddress: string;	Password: string;		constructor(public wakanda: Wakanda){}	ngOnInit(){}		getTravelers()	{		this.wakanda.getCatalog().then(ds =>		{			ds['Travelers'].query().then(collection =>			{				this.travelers = collection.entities;			});		});	}		newTraveler()	{	debugger  		this.wakanda.getCatalog().then((ds) =>   		{			let traveler = ds['Travelers'].create(				{					firstNames: this.firstname || '',					surname: this.surname || '',					phoneMobile: this.mobile || '',					phoneHome: this.homePhone || '',					passportNo: this.passportNumber || '',					passportIssuePlace: this.passportCountry || '',					birthCountry: this.birthCountry || '',					birthDate: this.dateOfBirth || '',					emailAddr: this.emailAddress || '',					password: this.Password || '',				});							traveler.save().then(()=>				{					alert('Saved');					this.travelers.push(traveler);						// {						// 	firstNames: this.firstname,						// 	surname: this.surname,						// 	phoneMobile: this.mobile,						// 	phoneHome: this.homePhone,						// 	passportNo: this.passportNumber,						// 	passportIssuePlace: this.passportCountry,						// 	birthCountry: this.birthCountry,						// });					this.firstname ="Saved";				});				});	}}