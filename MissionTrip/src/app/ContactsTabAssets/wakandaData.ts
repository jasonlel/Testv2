import { Component } from '@angular/core';import {Wakanda} from '../wakanda.service';import { Observable } from 'rxjs/Observable';import { BehaviorSubject } from 'rxjs/BehaviorSubject';import { DataSource } from '@angular/cdk/collections';import 'rxjs/add/observable/of';import 'rxjs/add/observable/fromPromise';export class ContactsDS extends DataSource<any>{	constructor(private wakanda: Wakanda)	{		super();	}		connect(): Observable<any>	{		return Observable.fromPromise(this.getContacts());	}	disconnect(){}		getContacts()	{		debugger		return this.wakanda.getCatalog().then(ds =>		{			return ds['Contacts'].query().then (collection =>			{					return collection.entities;			});		});	}		}